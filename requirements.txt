# Runtime dependencies. setup.py is the source of truth; this file exists so
# `pip install -r requirements.txt` works for anyone who reaches for it first.
#
# Install the package itself with `pip install -e .`, and the development
# tooling with `pip install -e ".[dev]"` (pytest, pytest-cov, mypy, black,
# flake8) rather than from here.

typing-extensions>=4.8.0   # typing back-ports for 3.10
lxml>=4.9.0                # parses the .hdt templates and .hdc characters
