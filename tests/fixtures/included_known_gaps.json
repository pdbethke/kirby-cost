{
  "_comment": "SHRINK-ONLY. Cells where the engine's included() disagrees with Hero Designer's, with what each side said. Written once as the 2026-08-29 baseline before any fix; every later commit may only remove entries. The target is empty.",
  "_baseline": "3770 of 8395 cells disagree (2026-08-29); now 9 remain (2026-08-30)",
  "gaps": {
    "COSTSEND-on-CUSTOMPOWER": "HD's CustomPower prototype reports getEndUsage() > 0 where the engine's reports 0; CustomPower.getActiveCost() (CustomPower.java:53-79) is baseCost plus adders and Main6E states BASECOST=0, so the source of HD's END is not yet identified; follow-up; lead: Main6E gives BASECOST=0 LVLCOST=1 LEVELSTART=1 and CustomPower.getActiveCost() (CustomPower.java:53-79) drops the levels term -- check how the oracle builds the prototype",
    "COSTSENDTOMAINTAIN-on-CUSTOMPOWER": "HD's CustomPower prototype reports getEndUsage() > 0 where the engine's reports 0; CustomPower.getActiveCost() (CustomPower.java:53-79) is baseCost plus adders and Main6E states BASECOST=0, so the source of HD's END is not yet identified; follow-up; lead: Main6E gives BASECOST=0 LVLCOST=1 LEVELSTART=1 and CustomPower.getActiveCost() (CustomPower.java:53-79) drops the levels term -- check how the oracle builds the prototype",
    "INCREASEDEND-on-CUSTOMPOWER": "HD's CustomPower prototype reports getEndUsage() > 0 where the engine's reports 0; CustomPower.getActiveCost() (CustomPower.java:53-79) is baseCost plus adders and Main6E states BASECOST=0, so the source of HD's END is not yet identified; follow-up; lead: Main6E gives BASECOST=0 LVLCOST=1 LEVELSTART=1 and CustomPower.getActiveCost() (CustomPower.java:53-79) drops the levels term -- check how the oracle builds the prototype",
    "INHERENT-on-CUSTOMPOWER": "HD's CustomPower prototype reports getEndUsage() > 0 where the engine's reports 0; CustomPower.getActiveCost() (CustomPower.java:53-79) is baseCost plus adders and Main6E states BASECOST=0, so the source of HD's END is not yet identified; follow-up; lead: Main6E gives BASECOST=0 LVLCOST=1 LEVELSTART=1 and CustomPower.getActiveCost() (CustomPower.java:53-79) drops the levels term -- check how the oracle builds the prototype",
    "LINKED-on-LEAPING": "state-dependent; needs the authored-power matrix, spec \u00a78",
    "LINKED-on-RUNNING": "state-dependent; needs the authored-power matrix, spec \u00a78",
    "LINKED-on-SWIMMING": "state-dependent; needs the authored-power matrix, spec \u00a78",
    "PERSISTENT-on-CUSTOMPOWER": "HD's CustomPower prototype reports getEndUsage() > 0 where the engine's reports 0; CustomPower.getActiveCost() (CustomPower.java:53-79) is baseCost plus adders and Main6E states BASECOST=0, so the source of HD's END is not yet identified; follow-up; lead: Main6E gives BASECOST=0 LVLCOST=1 LEVELSTART=1 and CustomPower.getActiveCost() (CustomPower.java:53-79) drops the levels term -- check how the oracle builds the prototype",
    "REDUCEDEND-on-SHAPESHIFT": "prototype END: engine SHAPESHIFT prototype has active_cost==0 (cost lives in TARGETINGCOST/NONTARGETINGCOST) so end_usage==0 and ReducedEND.java:200-204 fires; HD's prototype has END; follow-up with the CUSTOMPOWER cells"
  }
}
